import 'package:flutter/cupertino.dart';

class Seal extends StatelessWidget {
  const Seal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("seal"),
    );
  }
}
